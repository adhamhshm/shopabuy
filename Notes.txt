[----- Study Notes -----]

- Section 2 - API basics
- get .net info > dotnet --info
- get .net template list 
    > dotnet new list
- create a main/root folder for the application
- create a new solution file for .net in the main folder 
    > dotnet new sln
- see .sln file will be created
- see list of settings for web api template 
    > dotnet new webapi -h
- create an API/ folder in main/root folder with controller template 
    > dotnet new webapi -n API -controllers
- add the project to the solution file 
    > dotnet sln add API/
- run API project > dotnet run
- try open the API localhost url, you see nothing, but in API/ folder, we have Controllers/WeatherForecastController.cs
- go to [localhost-url]/WeatherForecast, should see something related to the code in the WeatherForecastController.cs file
- done with this demo part
- install dependency/extensions in vs code
    > C# Dev Kit, SQLite, SQLite Viewer, NuGet Gallery
- use "solution explorer" in the explorer tab/dropdown of the vs code
- use Program.cs as the main file for running .net application
- see appsettings.json and appsettings.Development.json, for development, the later is more significant
- see Properties/launchSettings.json for the app launch configuration, can retain the https
- remove connection not secure warning, run in API folder
    > dotnet dev-certs https --clean
    > dotnet dev-certs https --trust
- create Entities/ folder
- create Product.cs in the Entities/  folder
- use shortcut type "prop" / "propfull" [ENTER], to create property in a c# file

- Section 2 - using entity framework
- use NUGET, look in the cli terminal, in its tab (NuGet Gallery should be installed)
- search for "microsoft.entityframeworkcore" in NUGET
- select the Microsoft.EntityFrameworkCore.Sqlite, Microsoft.EntityFrameworkCore.Design
- choose the version of .NET you are using, and install to API.csproj
- create Data folder
- add new file StoreContext.cs, this class will be derived from DbContext
- use shortcut type "ctor" [ENTER], to create constructor in a c# file
- update Program.cs with DbContext services
- update appsettings.Development.json to include "ConnectionStrings:DefaultConnections"

- Section 2 - creating entity framework migration
- install dotnet-ef in API, find in browser for docs
    > dotnet tool install --global dotnet-ef --version 9.0.4
- check if installed
    > dotnet ef
- create initial migrations
    > dotnet ef migrations add InitialCreate -o Data/Migrations
- update database migrations
    > dotnet ef database update
- see store.db file is created, with SQLite Viewer, to be able to view the file

- Section 2 - adding data to database
- create a new class, DbInitializer.cs in the Data/ folder
- add mock data
- add DbInitializer method in Program.cs, to initialize data creation
- remove the existing created db
    > dotnet ef database drop
- run command to start the API again
    > dotnet watch
- create Controller/ folder
- add ProductsController.cs in the Controller/ folder, select API Controller template when creating a new file
- use postman for api testing
- use "async" in the ProductsController.cs, for best practices

Section 2 - source control with vs code
- open Source control tab in vs code (git should be installed)
- open main/root folder path, then run:
    > dotnet new list
- find this -> dotnet gitignore file | gitignore,.gitignore | Config
- use this template to have tidier files changes in Source control tab, this will add the gitignore file
    > dotnet new gitignore
- right click and add to .gitignore, from the list of files in the Source Control
- this is more on source control, not necessary to follow, but good to know

- react part
- use vite, typescript + swc
- can change port number in vite.config.js
- interesting setting for Linked Editing (vs code) refer to the <name></name>

- add cors in API setup
- change the client http to https
> cd client
> npm install vite-plugin-mkcert -D // -D means to just add this to dev dependency, not production
- go to vite.config.ts, add -> import mkcert from 'vite-plugin-mkcert'

- install mui from its docs
- install font and icons too

> npm install react-router-dom
- other react router stuffs may not be needed

- using redux
- install it as per in docs
- also install the react-redux (redux-toolkit)
> npm install react-redux
- create reducer, add store in the app folder, add Provider in main.tsx
- in the learning it teach about legacy redux and redux toolkit, refer the ContactPage.tsx
- can use redux dev tool
- create a custom based query using the baseApi.ts in api folder

- Error handling
- covered in API folder, create BaseApiController using API controller template, when creating new file
- learn how to setup exception from creating the middleware and use it in the program.cs
- then, create api endpoint in client folder -> about folder (example used), edit the store.ts
- error example is done in the about page
- show some error with react-toastify
- setup in the main.tsx and the baseApi.ts
- in main.tsx, need the css file for toaster to work, need to import "react-toastify/dist/ReactToastify.css"
- some ways to do error via a toast or error page, with new routes worth taking note for

- learning how to debug C# in vs code
- will need to create launch.json file from the vs code "Run and Debug"
- use -> {}.NET: Attach to a .NET process -> run when service already started
- use -> {}.NET: Launch a C# project -> run together with debugger
- place the break point in any of the C# code
- in "Run and Debug" select type of debug you want, then run

- Adding to cart
- First create a new entity in Entities folder
- then update the StoreContext.cs in Data/Migrations folder
- now in the API folder run this > dotnet ef migrations add BasketEntityAdded
- if table not yet created after build the migrations, can remove migrations with > dotnet ef migrations remove
- edit the entity class as per your references like relation/foreign key stuffs
- check the migrations file created, check for things like, table name, referential rule, nullable stuff etc
- now can run > dotnet watch
- the table should be created as per the migrations folder, in the store.db
- now create BasketController class
- now create the DTO classes
- add extensions for reusable DTO
- now move the client, add basket models
- create basketApi.ts in features/basket folder
- update the store.ts to include the new basket api, and its reducer and middleware
- create the basket page and component class
- edit routes to allow access to basket page
- edit the cart icon in navbar to include a link when click
- now implementing the add to card thingy
- check for cors in program.cs to allow cookie thingy
- also modify the baseApi.ts in client side
- create the basket component as needed
- now need to solve the caching issue as not every page need cache, addding the tags
- now update the navbar count for add to cart
- make all necessary changes to catalog and basket folder files
- create the checkout page, later will update the functionalities
- also cater the issue of adding to basket for first time user

- now working on sorting, filtering and searching of products, will update the product controller in API
- work on the API first and then, the client later
- update the feature folder that is calling the api like catalog etc
- update controller, update api (client), update slice, update store

- learning about identity with ASP.NET, login, register
- there is a link, how to use Identity to secure web api backend for SPAs
- in NUGET, install > Microsoft.AspNetCore.Identity.EntityFrameworkCore @Microsoft
- then create User file in the Entitites folder in API
- setup the program.cs, StoreContext.cs, DbInitializer.cs
- now can create a new migration, make sure in API folder, can stop the API first
> dotnet ef migrations add IdentityAdded
- now in the data folder we will have the Identity migrations
- now run the dotnet watch to initiate the data creation > dotnet watch
- can drop the database, the store.db will be gone
> dotnet ef database drop
- if run the "> dotnet watch" again, store.db will be created back
- add AccountController.cs, setup the api endpoint for register, login etc
- add Address entity, then, need to update migrations
> dotnet ef migrations add AddressAdded
- can test the endpoint using postman
- now adding the accountApi in the client side
- add accountApi.ts, User.ts, LoginSchema.ts, edit the store.ts as usual
- add form component in the account folder
- use zod and react-hook-form to add some form validation
- setup some persisting login session, in accountApi.ts and so on
- create data schema for login and register

- Section 10 - using stripe
- create stripe account
- add stripe dependency in API if not yet added
    > dotnet add package Stripe.net or use NuGet
- create Services folder
- create PaymentsService.cs in Services/ folder
- Add PaymentsService in Program.cs
- update migrations
    > dotnet ef migrations add BasketEntityUpdated
- see Data/Migrations/ folder
- create Controller/PaymentsController.cs
- update Basket entity, BasketExtensions, BasketDto with updated properties/fields for payment
- update the CheckoutPage in the frontend
- create its components in feature/checkout
- create lib/hooks/useBasket.ts
- update some existing class to accomodate stripe integration

- Section 11 - added Order feature
- create OrderAggregate/ folder
- create all classes needed in that Entities/OrderAggregate/ folder
- update Data/StoreContext.cs with the DbSet<> property
- update migrations
    > dotnet ef migrations add OrderEntityAdded
- see data in Data/Migrations/ folder
- run API again, and store.db should be updated
- create OrdersController in the Controllers/ folder
- create ClaimsPrincipalExtensions in the Extensions/ folder
- create CreateOrderDto.cs in the DTOs/ folder
- update the migrations
    > dotnet ef migrations add OrderEntityUpdated
- restart the app and database will be updated
    > dotnet watch
- create OrderItemDto.cs in the DTOs/ folder
- create OrderExtensions in the Extensions/ folder 
- create Order.ts in the src/app/models/ folder
- create features/order/ folder
- create orderApi.ts in the order/ folder
- update the store/store.ts to include the updated order reducer and middleware
- update some methods in the lib/util.ts
- install date-fns
    > npm install date-fns
- update CheckoutStepper.tsx with the "createOrder" mutation
- update CheckoutSuccess.tsx component
- update routes/Routes.tsx
- add stripe webhooks in the PaymentsController.cs

- Section 11 - stripe webhooks
- need to use stripe cli and webhook to get the WhSecret
    > stripe login
- go the the link and complete authorization
    > Press Enter to open the browser or visit https://dashboard.stripe.com/stripecli/confirm_auth?
    > Done! The Stripe CLI is configured for Shopabuy with account id acct_xxxxxxxxxx
    > Please note: this key will expire after 90 days, at which point you'll need to re-authenticate.
- use the listen command, after run this command, let the stripe clie run
    > stripe listen -f https://localhost:5001/api/payments/webhook -e payment_intent.succeeded,payment_intent.payment_failed
    > Ready! You are using Stripe API Version [2024-09-30.acacia]. Your webhook signing secret is whsec_xxxxxxxxxx
- check if payment webhook is working
    > 2025-07-23 09:47:48   --> payment_intent.succeeded [evt_xxxxxxxxxx]
    > 2025-07-23 09:47:48  <--  [200] POST https://localhost:5001/api/payments/webhook [evt_xxxxxxxxxx]

- Section 12 - Publishing/Deploying
- update the app code to finalize some stuffs
- update vite.config.ts to include build option
- add env.production
- check the package.json scripts -> build command
- run build command
    > npm run build
- see in the API/ folder, there is wwwroot/ folder
- update Program.cs to have "files" mapping in the middleware
- refresh the app
    > dotnet watch
- open https://localhost:5001/ and the app will be running showing web pages 
- refresh the page and the app will return "page can't be found"
- create FallbackController.cs in the Controllers/ folder
- update Program.cs to have the fallback controller
- open https://localhost:5001/ and the app will be running showing web pages and can be refreshed

- Section 12 - adding sql server
- create a docker-compose.yaml to run sql server via docker, if not installed locally
- update the appsetinggs.Development.json from "Data source=store.db" to "Server=localhost,1433....."
- install in NUGET > Microsoft.EntityFrameworkCore.SqlServer
- remove sqlite dependency in the API.csproj
- restore the dotnet build
    > dotnet restore
- update the services in Program.cs to use the sql server
- remove the Data/Migrations/ folder, as created migrations is referenced to the sqlite
- create new migrations for sql server, command below also added the folder path
    > dotnet ef migrations add SqlServerInitial -o Data/Migrations
- see in the Data/Migrations/ folder we now have all the migrations for everything we created
- update the DbInitializer.cs and the Program.cs, refer version control for changes
- drop the database 
    > dotnet ef database drop
- run the app again
    > dotnet watch
- update the CheckoutStepper.tsx, refer version control
- build the react app again as there is changes in the frontend part

- Section 12 - azure deployment
- need to use card payment huhu
- (all azure deployment configuration is skipped for now...)
- publish .net app
    > dotnet publish -c Release -o .bin/Publish
- see the API/bin/Publish folder is created
- can use the Azure App Service extension from vs code, the extension will appear
- right click the Publish/ folder and "Deploy to Web App"
- app should we working on the new deployed url
- some bug fixes in orderApi.ts, need invalidate the cache
- add tagTypes[] to the api etc

- Section 13 - CRUD and Roles
- switch to a new branch
    > git checkout -b feature/inventory
- create CreateProductDto.cs in the DTOs/ folder
- update ProductsCotroller.cs with new methods
- add automapper dependency (manual mapper is always better)
- in NUGET, install > AutoMapper @ Jimmy Bogard, AutoMapper.Extensions.Microsoft.DependencyInjection @Jimmy Bogard
- create MappingProfiles.cs in RequestHelpers/ folder
- update with IMapper in the ProductsController.cs parameter
- create UpdateProductDto.cs, update controller

- Section 13 - usign cloudinary
- use the cloud name, api key, api secret
- create CloudinarySettings.cs
- add services configration of the cloudinary in Program.cs
- install in NUGET > CloudinaryDotNet
- create ImageService.cs
- update the Program.cs with image service
- update Product.cs with new properties
- update migrations
    > dotnet ef migrations add PublicIdAdded
- see data in Data/Migrations/ folder
- create new feature/admin/ folder, create all the components in it
- create some reusable components for the form in app/shared/components/ folder - AppDropzone, AppSelectInput, AppTextInput
- create the adminApi.ts, update the store.ts to include the adminApi
- create handleApiError methods in util.ts


